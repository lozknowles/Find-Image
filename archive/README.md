# Archive

This folder preserves older `find_image*.py` iterations from the original exploratory workflow.

Use the root-level `find_image.py` for current work. The archived scripts are kept for reference only and may depend on older assumptions, local files, or experimental packages.
